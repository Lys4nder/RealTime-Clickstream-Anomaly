services:
  clickstream-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: clickstream-frontend
    ports:
      - "4200:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
